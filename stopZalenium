pipeline {
    agent any
    tools {
        maven "maven3.6.2"
    }
    stages {
        stage ('StopZalenium'){
             steps {
                //sh 'docker stop zalenium'
                sh 'sh docker_kill.sh'
            }
        }
    }
}   
